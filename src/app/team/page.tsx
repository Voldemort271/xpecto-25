import React from "react";
import { Handjet } from "next/font/google";

const handjet = Handjet({ subsets: ["latin"] });

const TeamPage = () => {
  return (
    <div className="min-h-screen w-screen border-2 border-amber-50 bg-neutral-900">
      hi
    </div>
  );
};

export default TeamPage;
